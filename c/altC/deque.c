#include <stdio.h>
#include <stdlib.h>

struct dbl_item {
    double data;
    struct dbl_item *prev, *next; 
};

struct dbl_item *createDeque()
{
    struct dbl_item *first = NULL, *last = NULL;
    struct dbl_item *current = NULL, *tmp;
    
}

int main()
{
    
    return 0;
}